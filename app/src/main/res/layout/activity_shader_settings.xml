<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".views.activities.ShaderSettingsActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarId"
        android:layout_width="match_parent"
        app:elevation="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/transparent">

        <com.google.android.material.appbar.MaterialToolbar
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:contentInsetLeft="0dp"
            android:contentInsetStart="0dp"
            app:contentInsetLeft="0dp"
            app:contentInsetStart="0dp"
            android:contentInsetRight="0dp"
            android:contentInsetEnd="0dp"
            app:contentInsetRight="0dp"
            app:contentInsetEnd="0dp"
            app:elevation="0dp"
            app:layout_scrollFlags="scroll|enterAlways">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <androidx.cardview.widget.CardView
                    android:id="@+id/backButtonId"
                    android:layout_width="?attr/actionBarSize"
                    app:cardCornerRadius="32dp"
                    android:layout_marginStart="8dp"
                    android:clickable="true"
                    android:focusable="true"
                    app:cardElevation="0dp"
                    app:cardBackgroundColor="@android:color/transparent"
                    android:foreground="?android:selectableItemBackground"
                    android:layout_height="match_parent"
                    android:layout_alignParentStart="true">

                    <ImageView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:contentDescription="@string/back_button"
                        android:background="@drawable/back_white_24dp" />

                </androidx.cardview.widget.CardView>

                <TextView
                    android:id="@+id/collectionTitleId"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/shader_effects_title"
                    android:fontFamily="@font/montserrat"
                    android:textStyle="bold"
                    android:layout_centerInParent="true"
                    android:textColor="@color/material_light"
                    android:textSize="18sp"/>

            </RelativeLayout>

        </com.google.android.material.appbar.MaterialToolbar>

    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingBottom="54dp"
        android:layout_gravity="center_vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Active shader picker — tap opens a single-choice dialog
                 (None / Rain / Ripple / …), same pattern as Transition Effect. -->
            <com.droid2developers.liveslider.views.components.SettingsCardView
                android:id="@+id/activeShaderCard"
                android:layout_width="match_parent"
                android:layout_height="70dp"
                app:cardIcon="@drawable/slideshow_24dp"
                app:cardHeader="@string/shader_active"
                app:cardSubHeader="@string/shader_none"
                app:hasSwitch="false" />

            <!--View Separator-->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="@dimen/divider_vertical_margin"
                android:layout_marginBottom="@dimen/divider_vertical_margin"
                android:background="@color/color_divider" />

            <!-- Populated at runtime from the active shader's Constant.ShaderDef
                 param list — sliders/toggles vary per shader, so these rows are
                 built in code rather than duplicated per-shader XML. -->
            <LinearLayout
                android:id="@+id/paramsContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>
